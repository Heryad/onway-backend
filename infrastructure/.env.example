# ============================================
# INFRASTRUCTURE SERVICES - DEVELOPMENT
# ============================================
# Docker services configuration
# Copy this to .env in infrastructure/ folder

# PostgreSQL Configuration
POSTGRES_DB=onway_db
POSTGRES_USER=onway_user
POSTGRES_PASSWORD=change_me_postgres_password_dev

# Redis Configuration
REDIS_PASSWORD=change_me_redis_password_dev

# Typesense Configuration
TYPESENSE_API_KEY=change_me_typesense_key_dev

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_me_minio_password_dev

# Grafana Configuration
GRAFANA_ADMIN_PASSWORD=change_me_grafana_password_dev

# pgAdmin Configuration
PGADMIN_EMAIL=admin@onway.com
PGADMIN_PASSWORD=change_me_pgadmin_password_dev
